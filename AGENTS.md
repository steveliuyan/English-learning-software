# AGENTS.md

## 项目使命

构建一款 Android 英语背词应用：以本地优先的词书学习、FSRS 间隔复习和每日 AI 语境短文为核心；面向全人群，首要服务大学生、四六级与考研用户。用户可自行配置兼容 OpenAI 的 AI 服务，用于文章、翻译、拍照翻译和可选云端发音。

## 工作方式

本仓库按“AGENTS.md + Skill + 分阶段 Spec”推进。执行任何代码修改前，必须：

1. 阅读本文件。
2. 阅读本阶段对应的 `docs/specs/` 规格文件及其前置阶段规格。
3. 加载并遵循任务相关 Skill；实现功能/修复缺陷时先加载 TDD Skill。
4. 先编写或更新测试，再写最小实现；每个阶段完成后运行本阶段定义的验收测试。
5. 未通过验收不得标记阶段完成；任何范围变化先更新对应 Spec，再实施。

## 规格路线图

| 阶段 | Spec | 交付目标 | 前置条件 |
|---|---|---|---|
| 0 | `docs/specs/00-foundation-and-architecture.md` | Android 工程、离线存储、安全基线、依赖与许可台账 | 无 |
| 1 | `docs/specs/01-vocabulary-learning-and-review.md` | 词书、今日计划、词卡、三档反馈、FSRS 复习 | 阶段 0 |
| 2 | `docs/specs/02-reading-and-ai-content.md` | 每日文章解锁、双语阅读、AI 配置与受控请求 | 阶段 1 |
| 3 | `docs/specs/03-dictionary-pronunciation-and-photo-translation.md` | 查词、TTS、AI 音频缓存、OCR 与拍照翻译 | 阶段 0；查词与文章联动依赖阶段 2 |
| 4 | `docs/specs/04-history-reminders-backup-and-security.md` | 历史统计、提醒、备份迁移、认证恢复 | 阶段 0、1、2；音频备份依赖阶段 3 |
| 5 | `docs/specs/05-release-quality-and-compliance.md` | 无障碍、性能、离线测试、隐私、许可证与发布检查 | 阶段 0–4 |

`docs/superpowers/specs/2026-09-17-android-ai-english-vocabulary-app-prd.md` 是产品总 PRD；分阶段 Spec 是开发时的可执行范围与验收依据。若两者冲突，以较严格的安全、隐私和验收要求为准，并在 `docs/decisions/` 记录决策。

## 强制产品约束

### 本地优先

- 词书、学习记录、FSRS 复习、今日计划、历史文章和已缓存媒体以本地数据为权威。
- 学习反馈必须先成功写入本地，网络或 AI 调用不得阻塞提交反馈。
- 无网络或无 AI Key 时，背词、复习、离线查词、已缓存文章阅读和系统 TTS 仍应可用。
- 无账户、无隐式云同步。首次使用和备份页面必须明确：卸载、清除数据或设备损坏前未导出数据，可能无法恢复。

### 学习与解锁规则

- 每日计划在首次进入学习时，按设备本地时区的 `00:00` 与活动词书生成不可变快照：`newTarget`、`dueTarget`、任务集合和解锁规则。
- 每张卡提交一次有效的“不认识 / 模糊 / 认识”反馈即记为该计划项完成；同一事件不可重复计数。
- 严格模式：`newDone == newTarget && dueDone == dueTarget` 时解锁；`newTarget == 0` 或 `dueTarget == 0` 对应条件视为已满足。
- 宽松模式：`newDone == newTarget` 时解锁；到期复习仍保留待办。
- 当日计划生成后的新到期词不加入该计划；历史已解锁文章始终可重读。未解锁只限制当天新文章生成入口。
- 设置默认从次日生效；当天未开始时可由用户明确选择重建计划，已提交反馈后不得静默重建。
- 词书各自维护独立进度。切换词书不得删除旧记录；同一 lemma 跨词书默认不合并进度。

### AI 与隐私安全

- API Key 只保存在 Android Keystore 支撑的安全凭据区；不得出现在 Room 业务表、日志、崩溃报告、剪贴板、导出包、文章元数据或截图诊断。
- 仅支持 HTTPS 公网 Endpoint；拒绝 HTTP、`file:`、`content:`、环回地址、私网地址、重定向至这些地址的请求及自签名证书。
- Endpoint、模型、能力声明与 Key 必须绑定在同一 AI Profile；切换 Endpoint 不得复用其他 Profile 的 Key。
- V1 高级参数白名单：`temperature`（0–2）、`top_p`（0–1）、`max_tokens`（1–4096）、`timeout_seconds`（5–120）和受控系统提示模板。拒绝任意 Header、任意请求体覆盖与任意工具调用。
- 首次调用第三方 Endpoint 前展示域名及“文本/图片和 Key 将发送给该服务”的确认。图片发送每次都需明确确认。
- AI 输出和导入内容均为不可信输入；必须做大小、结构、语言、长度与安全校验，并以无脚本富文本方式渲染。

### 发音、OCR 与资源

- 发音优先级：系统 Android TTS（离线英语包）→ 端侧 sherpa-onnx/Piper 英语模型 → TalkifyTTS 或用户配置的在线 TTS Provider。
- 系统 TTS 不生成可导出音频。AI 音频缓存需记录内容哈希、来源、模型版本和关联实体；损坏或替换后不得播放或导出。
- OCR 只负责本地文字识别与坐标；翻译是独立步骤。图片默认不保存；上传 AI 前必须确认。
- 未通过许可核验的词书、词典、音频或模型不得打包、下载或发布。

### 备份与恢复

- 备份可包含学习数据、文章、收藏、查询历史、可选 AI 音频和用户主动保存的拍照翻译记录；绝不包含 API Key、Keystore 密钥、系统 TTS 数据或临时图片。
- 安全恢复必须使用旧设备一次性挑战响应配对 + 完整性签名 + 可选备份密码。静态二维码、仅文件内公钥或“跳过认证”均不可作为替代。
- 密码保护必须是经审查的 AEAD 加密与带盐、内存硬 KDF；具体算法与参数由安全设计阶段确定，禁止自创加密。
- 导入只能处理受支持的白名单格式，必须限制文件大小、条目数、解压比例和路径；防御 Zip Slip、路径穿越、畸形数据和资源炸弹。
- 所有验证必须在隔离临时区完成。签名、配对、密码、版本、哈希、资源关系、空间或格式任一失败：拒绝恢复、清理临时区、正式数据零写入。
- V1 仅支持“替换本机数据”，不支持合并。恢复前创建回滚点，恢复后重建提醒与索引，要求重新填写 API Key。

### Android 与质量

- 目标平台和最低 Android API 由阶段 0 锁定；必须支持小屏、动态字体、深色模式与 TalkBack 基础导航。
- Android 13+ 提醒必须处理 `POST_NOTIFICATIONS`；拒绝后不反复弹窗。默认关闭提醒；每天最多一次，不做补发。非精确提醒允许延迟并在设置中说明。
- 核心路径不得阻塞 UI；网络请求可取消、超时并映射为可行动错误。
- 每个第三方依赖、数据集和模型必须登记版本、许可证、用途、数据流、NOTICE 位置与替代方案。

## 技术方向（待阶段 0 锁定版本）

- Kotlin、Jetpack Compose、Room、WorkManager / AlarmManager。
- FSRS Kotlin 实现负责复习调度；保留原始学习事件，算法升级只影响未来安排。
- Android Keystore 负责机密材料；SQLCipher 是否默认启用由性能验收决定。
- 离线词典候选：OpenLeaf-Dictionary SQLite 或经过许可核验的等价数据。
- OCR：ML Kit 为主、Tesseract 为无 GMS 降级。
- 离线高质量发音：sherpa-onnx + 经许可核验的 Piper 英语模型。

## 文档与交付规则

- 规格文件使用中文，需求用稳定 ID（如 `F1-01`）和可自动/手工验证的验收标准。
- 新的架构、依赖、安全或产品决策写入 `docs/decisions/YYYY-MM-DD-<topic>.md`，说明背景、候选方案、决定和影响。
- 每个阶段开始前创建该阶段实现计划：`docs/superpowers/plans/YYYY-MM-DD-<stage-name>.md`。
- 每个阶段的验收测试、质量审查与阶段文档均完成后，必须创建可恢复的 Git 提交并推送至 GitHub 远程仓库作为异地备份；推送前确认远程仓库地址、目标分支和认证可用，不得以未验证的推送宣称备份完成。若 GitHub 推送失败，必须保留本地提交、报告失败原因并在修复后重试。
- 不在没有用户确认的情况下增加账号、云同步、广告、订阅、社交、任意 AI 请求覆盖或远程代码执行能力。
